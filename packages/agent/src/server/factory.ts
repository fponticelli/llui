import type { ServerOptions, AgentServerHandle } from './options.js'

/**
 * Creates an LLui agent server. HTTP endpoints land in Plan 4 Tasks 8-13.
 * This stub satisfies the type-check surface exported by index.ts.
 */
export function createLluiAgentServer(_opts: ServerOptions): AgentServerHandle {
  throw new Error('createLluiAgentServer: not yet implemented — see Plan 4 Tasks 8-13')
}
