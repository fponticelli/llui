// @llui/compiler-ssr — SSR support (opt-in).
//
// This package owns the SSR-specific transforms that @llui/vike
// invokes during server-side rendering. The transforms handle the
// 'use client' directive, hydration boundaries, and the SSR-specific
// emission paths (renderToString-friendly output).
//
// Activation: opt-in via `ssr()` factory in llui.config.ts.
//
// Modules move here in v2c/decomp-28 (scaffolding lands first in
// v2c/decomp-24; this file is the placeholder index).

export {}
