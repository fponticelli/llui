// Consumer-owned lookalike: its basename deliberately matches @llui/dom's
// defining source module. Provenance, never spelling, decides helper identity.
export const tagSend = (..._args) => undefined
