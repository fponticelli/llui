{
  "extends": "../../tsconfig.json",
  "include": ["src", "test"],
  "exclude": ["**/*.prototype.ts"],
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    // test/packaging.test.ts asserts this package's dependency declarations against
    // @llui/markdown-editor's peer set by importing both manifests directly.
    "resolveJsonModule": true
  }
}
