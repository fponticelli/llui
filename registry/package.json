{
  "name": "@llui/registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "LLui component registry — shadcn-style source items copied into consumer projects by `llui add`. Private: it is distributed as JSON from llui.dev, never as an npm package.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint llui",
    "test": "vitest run"
  },
  "devDependencies": {
    "@llui/components": "workspace:*",
    "@llui/dom": "workspace:*",
    "@llui/interactions": "workspace:*",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.0.0",
    "tw-animate-css": "^1.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.2"
  }
}
